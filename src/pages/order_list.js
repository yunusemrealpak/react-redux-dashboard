import React from 'react';

const OrderList = () => {

    // const test = useSelector(state => state.testReducer);
    // const name = useSelector(state => state.testReducer.username);
    // const dispatch = useDispatch();

    return (
        // <div>
        //     {test.username}
        //     <button onClick={changeDispatch}>Değiştir</button>
        // </div>
        <h1>Order List Page</h1>
    )
}

export default OrderList;